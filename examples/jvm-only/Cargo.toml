[package]
name = "uniffi-kmm-example-jvm-only"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "uniffi_kmm_example_jvm_only"
crate-type = ["lib", "cdylib", "staticlib"]
path = "src/commonMain/rust/lib.rs"

[dependencies]
uniffi = { workspace = true }
